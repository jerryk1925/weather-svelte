cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - node_modules/

pages:
  image: node:latest
  stage: deploy
  script:
    - yarn install
    - yarn build
    - cd dist
  artifacts:
    paths:
      - public
  only:
    - master
